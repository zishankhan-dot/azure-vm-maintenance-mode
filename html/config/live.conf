server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    root /var/www/live;
    index live.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
